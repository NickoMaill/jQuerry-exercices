$(() => {

    $(".btn").click(function() {

        const text = $("#text");
        text.show();

        //la methode .show pourrait correspondre a un display: unset (même si cependant il n'existe pas encore d'opposé a display: none) 
        //ou à visibility: visible.


    });

});